{
  "version": 2,
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.jsx"
    }
  ],
  "builds": [
    {
      "src": "src/",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ]
}
