{
  "version": 2,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/root.jsx"
    }
  ],
  "builds": [
    {
      "src": "src/",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ]
}
