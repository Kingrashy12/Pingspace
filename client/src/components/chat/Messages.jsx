import React from "react";

const Messages = ({ chat }) => {
  return (
    <div class="flex flex-col relative overflow-x-auto mt-16 text-white p-5">
      Messages
    </div>
  );
};

export default Messages;
